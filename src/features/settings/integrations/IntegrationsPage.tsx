import * as React from 'react';
import { SettingsShell } from '../SettingsShell';

/** Stub — built in a later slice. See the plan's build order. */
export function IntegrationsPage() {
  return (
    <SettingsShell activeItem="Integrations" title="Integrations">
      <div className="[font:var(--text-body-3)] text-[var(--color-text-secondary)]">
        Coming in a later slice.
      </div>
    </SettingsShell>
  );
}
